# .github/workflows/ci.yaml
name: ci

on: push

jobs:
  tests:
    runs-on: ubuntu-latest
    name: npm tests
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
      - run: npm install
      - run: npm test
